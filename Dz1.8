def fnc(a):
    q=1
    for i in range (1,7):
        q=q*(a%10)
        a=a//10
    print("Произведение цифр числа равно:",q)

a=int(input("Введите число:"))
if a>=100000:
    fnc(a)
else:
    print("Это не шестизначное число, попробуйте еще раз")
