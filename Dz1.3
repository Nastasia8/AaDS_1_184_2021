from math import *
def func(x,y,sumbol): 
    if sumbol==1:
        print(x+y)
    if sumbol==2:
        print(x*y)
    if sumbol==3 and y!=0:
        print(x/y)
    else:
        print("y не может быть равен нулю, попробуйте еще раз")
    if sumbol==4:
        print(x-y)
    if sumbol==5:
        print(x**y)
x=int(input("Введите x: "))
y=int(input("Введите y: "))
sumbol=int(input("Введите sumbol: "))
func(x,y,sumbol)

