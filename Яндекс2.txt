'''

                            Online Python Compiler.
                Code, Compile, Run and Debug python program online.
Write your code in this editor and press "Run" button to execute it.

'''

a = int(input())
B = [0]*a 
   for i in range(a):
       B[i] = list(map(int, input().spiit()))
B.sorted(key = lambda x: x[0])
B.sorted(key = lambda x: x[1], reverse = 1)
   for i in range(a):
       print(B[i][0], B[i][1])
