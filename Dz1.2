from math import *
def period (l,g=9.8):
    if l>0:
        T=2*pi*sqrt(l/g)
        print(T)
    else:
        print("Длина не может быть меньше 0, попробуйте еще раз")
l=int(input("Введите длину нити маятника:"))
period(l)
